{{- if and .Values.sso.enabled .Values.sso.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sso.postgresql.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "taskflow.componentLabels" (dict "root" . "component" "sso-db") | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for StatefulSet
  ports:
  - port: {{ .Values.sso.postgresql.service.port }}
    targetPort: postgres
    protocol: TCP
    name: postgres
  selector:
    {{- include "taskflow.componentSelectorLabels" (dict "root" . "component" "sso-db") | nindent 4 }}
{{- end }}
