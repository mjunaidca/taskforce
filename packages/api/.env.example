# TaskFlow API Environment Variables

# Database (Neon PostgreSQL)
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://neondb_owner:xxx@ep-xxx.us-east-1.aws.neon.tech/dev?sslmode=require

# Better Auth SSO
# The base URL of your Better Auth SSO server
SSO_URL=http://localhost:3000

# Security
# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Debug mode (set to false in production)
DEBUG=true

# Dev mode - bypasses JWT auth for local development
# Set to true to skip token verification (uses mock user)
DEV_MODE=false
DEV_USER_ID=dev-user-123
DEV_USER_EMAIL=dev@localhost
DEV_USER_NAME=Dev User

# ChatKit Store (separate schema for conversations)
TASKFLOW_CHATKIT_DATABASE_URL=postgresql://neondb_owner:xxx@ep-xxx.us-east-1.aws.neon.tech/chat?sslmode=require

# MCP Server URL (default: http://localhost:8001/mcp)
MCP_SERVER_URL=http://localhost:8001/mcp

# OpenAI API Key
OPENAI_API_KEY=sk-proj-