apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskflow-learning.fullname" . }}
  labels:
    {{- include "taskflow-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskflow-learning.selectorLabels" . | nindent 4 }}
