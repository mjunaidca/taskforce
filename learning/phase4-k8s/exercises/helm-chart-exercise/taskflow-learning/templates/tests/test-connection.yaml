apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taskflow-learning.fullname" . }}-test-connection"
  labels:
    {{- include "taskflow-learning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taskflow-learning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
